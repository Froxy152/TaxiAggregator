server:
  port: 8080
spring:
  application:
    name: passenger-service
  datasource:
    driver-class-name: ${DB-DRIVER}
    url: ${ DB-URL }
    username: ${DB-USERNAME}
    password: ${DB-PASSWORD}
  jpa:
    database: ${DB}
    hibernate:
      ddl-auto: update

